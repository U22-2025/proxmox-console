name: Discord Issue Notify
 
on:
  issues:
    types: [opened, closed, deleted]
 
jobs:
  notify:
    uses: Cinnamobot/discord-issue-notify/.github/workflows/notify.yaml@main
    secrets:
      DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
 
